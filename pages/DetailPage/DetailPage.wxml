

<view class = "classname"> 
  <text>{{activityTitle}}</text>

<!-- <view bindtap="haveSave">
    <image class="save " src="{{isClick?'/icon/in_jiangjin_Fs.png':'/icon/in_jiangjin.png'}}" ></image>

    <text class="saveText">{{isClick?'取消':'收藏'}}</text> -->

</view>


<view class = "date"> 
  <text>时间：{{beginTime}}
  </text>
</view>
<view class = "location"> 
  <text>地点：{{building}} {{room}}
  </text>
</view>

<view class ="subtitle">
  <text>简介</text>
</view>

<view class ="context">
  <text>{{activityDesc}}</text>
</view>

<view class ="subtitle">
  <text>详情</text>
</view>

<view class ="context">
    <rich-text nodes = "{{activityDetail}}"> </rich-text>  
</view>
<view class ="comment">
  <text>评论</text>
</view>

<scroll-view scroll-y="true" class="container" upper-threshold="10" lower-threshold="5" bindscrolltolower="lower"  scroll-into-view="{{toView}}" scroll-top="{{scrollTop}}">

<block wx:for="{{fake}}" wx:for-index="idx" wx:for-item="item" data-idx="{{idx}}">

    <view class="feed-content">


      <view class="question" qid="{{id}}" bindtap="bindItemTap" data-activityId="{{item.userName}}">
        <a class="question-link">
          <text>{{item.comText}}</text>
        </a>
      </view>
      <view class="answer-body">
        
        <view bindtap="bindItemTap" data-activityId="{{item.comText}}">
      
          <text class="answer-txt" >{{item.userName}}</text>
        </view>
        <view class="answer-actions" bindtap="bindItemTap" data-activityId="{{item.userName}}">

        </view>
      </view>

   
    </view>
</block>


</scroll-view>

   <!--发表评论---->
   <view class="DoComment">
     <van-cell-group>
       <van-field
         model:value="{{ DoCommentValue }}"
         placeholder="输入你的评论……"
         border="{{ false }}"
       />
     </van-cell-group>
   </view>
   
<view class="DoComment">
<!--text>发表评论</text---->
 <button class="DoCommentBtn" bindtap="clickDoComment">评论/comment</button>
</view>

<view class="view-contain-ti1">
    
    <text class="text-ti1"></text>
    <input class="input1"></input>
</view>


<view class="s_view">
  <button class="submitBtn" disabled="" bindtap="clickStar">收藏/star</button>
      <button class="saveBtn" bindtap="clickSign" >报名/Sign 人数：{{registryNum}}/{{estimateNum}}</button>
</view>


